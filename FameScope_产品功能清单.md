# FameScope · 产品功能清单（最终版）

> 一句话定位：一个手机端 PWA 网站，帮你每天 5 分钟掌握你关注领域里最有影响力的人都说了什么、透露了什么信号。

---

## 一、产品概览

| 项目 | 说明 |
|---|---|
| 产品名 | FameScope |
| 核心价值 | 不只是聚合，而是从噪音里提炼"信号"——谁说了什么、为什么值得看 |
| 形态 | 手机端 PWA（可添加到主屏、离线缓存、Web Push），不做原生 App |
| 阶段 | 正式上线，有域名/服务器/注册，暂不做付费 |
| 支持领域 | 任意领域（AI、加密、太空科技、生物科技等），大领域颗粒度，不预置 |
| AI 引擎 | DeepSeek API（deepseek-v4-flash） |
| X 数据 | TwitterAPI.io（第三方），封装为可替换的 DataSource 接口 |
| 预估月成本 | 约 $15 + 域名年费 |

---

## 二、核心概念

### 信源（source）
追踪对象统称"信源"，分两类：

| 类型 | 说明 | 观点过滤 | 头像样式 |
|---|---|---|---|
| 个人（person） | 创始人、科学家等个人账号 | 需要（即兴回复/引用价值高，但水推多） | 圆形 |
| 官方（org） | 公司/机构官方 X 账号 + 官方博客 RSS | 不需要（都是正式公告） | 方角 |

**不预置任何信源**，一切由用户自己添加。

### 内容类型（content_type）
| 类型 | 来源 | 信息流 tab |
|---|---|---|
| tweet | X 推文（个人/官方账号） | 社交动态 |
| article | 官方博客 RSS | 官方新闻 |

### 数据模型分层（重要）

这是多用户个性化产品，数据必须拆成两层：

**内容层（所有用户共享）**

| 表 | 关键字段 | 说明 |
|---|---|---|
| `source` | type / 英文名 / 中文译名 / X账号 / 官博RSS / 三维分值 | 信源基础信息 |
| `post` | source_id / content_type / 标题 / 原文 / 中文摘要 / 评分 / 标签 / 领域 / 关系类型 / 媒体URL | **不含收藏** |

**用户行为层（每人独立）**

| 表 | 关键字段 | 说明 |
|---|---|---|
| `user_source` | user_id + source_id + notify + followed_at | 谁订阅了哪个信源、是否开启实时提醒、订阅时间 |
| `user_post_state` | user_id + post_id + is_starred | 每人独立的收藏状态 |
| `digest` | user_id + generated_at + content(JSON) | 每日简报归档，永久保留 |

**为什么这样设计：**
- A 和 B 共同订阅奥尔特曼 → `post` 只存一份，AI 也只处理一次，节省成本
- A 和 B 订阅不同的人 → 查询时通过 `user_source` 过滤，各自只看到自己订阅的内容
- A 收藏某条 → 写入 `user_post_state`，B 的状态完全独立不受影响

### 历史内容展示规则

订阅信源后**回溯展示过去7天内容**，不是从订阅时刻起，也不是全部历史：
- 查询条件：`post.created_at >= followed_at - 7天`
- 7天前的内容不出现在信息流，但全文搜索不受此限制

### 简报归档规则

简报永久保留，不删除。简报页显示为按日期倒序的列表，最新一期默认展开，往下滑看历史。简报和信息流共用 `user_post_state` 管理收藏状态。

---

## 三、功能清单

### A. MVP 首版必做

**1. 数据采集**
- TwitterAPI.io 接入（封装为 DataSource 接口，可替换）
- 采集个人 + 官方账号的原创推、回复推、引用推（纯转发不展示）
- 官方博客 RSS 订阅接入
- 所有采集数据持久化存入 Supabase

**2. AI 处理（DeepSeek deepseek-v4-flash）**

三种内容，三种不同处理方式，核心原则：**推文保留原味，新闻压缩摘要，简报按人提炼。**

*推文（tweet）— 直译，不提炼*
- 只做中文直译，不总结、不提炼
- 不展示"为什么值得看"信号提示（信号提示只用于新闻）
- 翻译自然流畅，口语保持口语感
- 保留原文入口供核对
- 内部仍打：重要性评分、话题标签、领域归属（系统内部用，不在推文卡片上展示信号提示）
- 观点过滤照常执行（判断回复/引用是否含实质观点）

*官方新闻（article）— AI 提炼摘要*
- 生成中文摘要：目标 150-200 字，硬上限 200 字
- 交代清楚"这篇文章说了什么"，按内容繁简弹性调整，不注水
- 新闻卡片展示信号提示（正式公告信号价值明确）
- 保留"阅读全文"跳转链接

*每日简报 — 按人物维度提炼*
- 按关注人物分节，每人一个小节，梳理其过去24小时内有价值的内容
- 示例结构：今日核心一句话 → 马斯克今天说了什么 → 奥尔特曼今天说了什么 → 官方新闻摘要
- 每节只列有实质价值的内容，过滤水推
- 简报有 AI 的判断和提炼，不是流水账

所有提示词集中放 `lib/prompts.ts`，三种处理对应三套提示词，独立调整。


**3. 信息流（主界面）**
- 顶部双 tab：
  - **社交动态** tab：X 推文卡片，过滤含"全部/收藏/含观点"，每个 tab 带未读数气泡
  - **官方新闻** tab：新闻卡片，过滤含"全部/收藏"
- 推文卡片：头像（圆）、中文译名·英文名、公司职位、时间、关系标注（原创/引用/回复）、**中文直译**（无信号提示）
- 新闻卡片：头像（方角）、机构名、来源标记、**信号提示**、配图（可选）、文章标题、AI 摘要（≤200字）、"阅读全文"跳转链接
- 两种卡片共享底部操作栏：原文 / 收藏 / 分享（三项并排）
- 媒体处理：图片懒加载引用原 URL；视频只显示封面+播放按钮，点击跳原平台，绝不下载转存

**4. 原文/译文切换**
- 每条内容保留"看原文"折叠入口，展开后显示英文原文（紫色竖线标记）

- 收藏（长期留存）

**6. 全文搜索**
- 检索历史动态（人物/话题/关键词），Postgres 全文索引

**7. 用户系统（轻量）**
- **MVP 阶段：邮箱验证码登录**（Supabase Auth 自带，零成本零资质）
- 手机号登录：需营业执照申请短信签名，暂不做
- 微信登录：列为后续迭代
- 保存个人的追踪列表、收藏

**8. PWA 基础**
- manifest.json + Service Worker 离线缓存 + "添加到主屏"

### B. 主链路跑通后接着做

**9. 推送系统（PWA Web Push，不做邮件推送）**

两种推送，都通过 PWA Web Push 发出：

*实时提醒（per 信源）*
- 用户对每个订阅的信源单独开启/关闭实时提醒
- 逻辑：采集到新内容 → AI 评分 → 给开启了该信源 notify 的用户推送
- `user_source` 表加 `notify` 字段记录开关状态
- 不依赖 Cron 频率，采集完立即触发检查

*每日简报（早8点 Cron）*
- 给所有用户逐个生成个性化简报（per-user 函数）
- 生成步骤：① 按用户订阅拉取过去24小时高分内容（≥6分，10-15条）→ ② DeepSeek 生成结构化摘要 → ③ 存库 → ④ Web Push 通知
- 简报结构：今日核心一句话 / 最值得关注 / 信号共振 / 官方新闻 / 你关注的人
- 简报存库，用户可随时回看历史
- 每条重点结论下方带「分享这条」入口

**10. 分享功能（增长核心）**
- 分享入口在每条动态/每条简报内容下方操作栏（不放在设置里）
- 分享卡片结构（从上到下）：
  - 顶部：FameScope 大字品牌名 + 领域副标题（随内容领域动态变化）
  - 中部：中文金句 + 英文原文对照
  - 人物区：通用中文译名 · 英文名 + 公司职位
  - 底部引流区：引导语 + 网站二维码（带渠道参数，可追踪来源）
- **分享文案随领域动态变化**（大领域颗粒度）：
  - AI → "追踪全球 AI 大佬动态"
  - 加密 → "追踪全球加密领域大佬动态"
  - 其他领域同理，配色也随领域走
- H5 分享落地页 + 微信 JS-SDK 配置

**11. 发现页（添加信源）**
- **单一搜索框**，自动识别输入是"领域"还是"具体名字"：
  - 识别为领域 → 推荐流程，双 tab（名人 / 官方账号），按三维度排序
  - 识别为具体名字 → 直接匹配账号，个人和官方混合显示
  - 搜索框下方显示识别结果提示，边界情况提供切换出口
- "我的追踪"列表管理已添加信源
- **推荐两步法**（必须遵守）：
  - Step 1：DeepSeek 召回候选名单（只取名称+身份+中文译名，不信任任何数字）
  - Step 2：TwitterAPI.io 核实真实账号（查不到直接丢弃），抓真实数据计算三维分值
  - 常见领域结果可缓存

**12. 推送精细控制**
- 每个订阅的信源可单独开启/关闭实时提醒（在"我的追踪"列表里操作）
- 默认关闭，用户主动开启才推，避免打扰

### C. v2 再做

- 稍后读队列（与收藏区分）
- 个人笔记/划线（第二大脑）
- 「我不在的这几天」回归摘要
- 话题串联/事件脉络时间线
- 辩论场（观点分歧可视化）
- 趋势热力图/数据看板
- 播客/演讲接入（语音转写）
- 社区互动（需用户规模 + 内容审核）

> 付费会员体系、独立后台管理 —— 明确暂不开发。

---

## 四、命名规范

### 公司名
一律保留英文，不翻译。如需辅助说明用括号注：Anthropic（Claude 母公司）。

**错误示范**："开放人工智能""克劳德开发商"——没人这么叫。

### 人物名（通用中文译名，非直译）

| 英文名 | 通用译名 | 显示格式 |
|---|---|---|
| Sam Altman | 奥尔特曼 | 奥尔特曼 · Sam Altman |
| Demis Hassabis | 哈萨比斯 | 哈萨比斯 · Demis Hassabis |
| Elon Musk | 马斯克 | 马斯克 · Elon Musk |
| Yann LeCun | 杨立昆 | 杨立昆 · Yann LeCun |
| Dario Amodei | 阿莫迪 | 阿莫迪 · Dario Amodei |
| Andrej Karpathy | 卡帕西 | 卡帕西 · Andrej Karpathy |

规则：中文译名在前，英文名在后。正文简称用中文姓氏（如"奥尔特曼"）。
简报写法示例："OpenAI 的奥尔特曼与 Meta 的杨立昆就 Scaling Law 再次交锋"。

---

## 五、技术栈

| 层 | 选型 | 说明 |
|---|---|---|
| 前端 | Next.js 14 + TypeScript + Tailwind CSS | 移动优先，SSR 利于分享预览 |
| PWA | next-pwa / Serwist | 离线缓存 + 添加到主屏 |
| 后端 | Next.js API Routes | 一体化，无需独立后端 |
| 数据库 | Supabase (Postgres) | 含内置 Auth |
| 认证 | Supabase Auth / 微信登录 | 验证码轻量登录 |
| 定时任务 | Vercel Cron | 采集 + 简报生成 |
| AI | DeepSeek API (deepseek-v4-flash) | OpenAI 兼容端点 |
| X 数据 | TwitterAPI.io | 封装为可替换接口 |
| 提示词管理 | lib/prompts.ts | 集中管理，方便调整 |
| 分享卡片 | @vercel/og | 服务端动态生成图片 |
| 部署 | Vercel + 自有域名 | 国内重度使用可换国内云+备案域名 |

---

## 六、媒体处理策略

**核心原则：只存文本和链接，媒体字节永远留在原平台。**

- 存（极轻）：文本、媒体 URL、缩略图 URL、宽高比
- 不存：图片原图、视频文件
- 图片：引用原 URL + `loading="lazy"` + 存宽高比防抖动
- 视频：只存封面图 URL，点击才跳转或嵌入原平台播放器
- 绝不下载/转存/转码视频
- 风险预案：图片防盗链时用 wsrv.nl 等代理中转

---

## 七、UI 设计规范

**原则：先完成所有页面 UI 并确认，再对接数据接口。**

做任何前端 UI 之前必须先调用 `frontend-design` 技能。所有颜色用 CSS 变量，严禁硬编码 hex 值。

### 双色模式
浅色默认，用户可切换深色。两套同时支持，CSS 变量自动适配。

### 品牌色
浅色主色 Purple 600（#534AB7），深色主色 Purple 400（#7F77DD）。信号提示用 Amber 系，收藏激活用 Amber 系，分享用 Info 系，

### 卡片操作栏（三个纯图标）
右对齐排列，38×38px，无文字标签：
- 原文 ti-language：默认态
- 收藏 ti-star：激活时 Amber 背景
- 分享 ti-share-3：Info 背景色

### 页面完成顺序
1. 信息流主页（社交动态 + 官方新闻双 tab）
2. 简报页（当日 + 历史列表）
3. 发现页（单搜索框 + 双 tab 推荐）
4. 个人设置页（追踪管理 + 实时提醒 + 色系切换）
5. 分享卡片弹层（中英对照 + 品牌顶部 + 二维码）
6. 登录/注册页
7. 新用户引导页（空状态）

### 核心规范
- 卡片圆角 12px，按钮 8px，胶囊标签 20px，个人头像圆形，官方头像 9px 方角
- 字体：品牌名 17px/500，人名 13px/500，正文 13px/400，新闻标题 15px/500，最小 10px
- 间距：页面边距 16px，卡片内边距 14px，卡片间距 10px
- 分享金句：serif 字体

---

## 八、后台管理

**MVP 阶段不做独立后台。**

| 需求 | MVP 阶段处理方式 |
|---|---|
| 调整 AI 提示词 | 修改 lib/prompts.ts，重新部署 |
| 查看/修改数据 | Supabase 自带可视化表格界面 |
| 采集是否正常 | 打开网站自己看 |

未来扩展：用户和内容量大了之后再考虑后台（采集监控、内容审核等）。

---

## 九、技术要点

1. **DeepSeek 模型名**：用 `deepseek-v4-flash`，旧别名 `deepseek-chat`/`deepseek-reasoner` 将于 2026-07-24 弃用
2. **省成本**：固定系统提示词放 prompt 开头，利用上下文缓存折扣（约 90%+）
3. **接口兼容**：DeepSeek 提供 OpenAI/Anthropic 兼容端点，换模型几乎零改动
4. **X 数据风险**：处于服务条款灰色地带，采集层做成可替换接口，不写死
5. **国内访问**：Vercel 偶尔不稳定，重度使用考虑国内云+备案域名；微信内分享需配置 JS-SDK

---

## 十、开发节奏（约 7-8 周，业余推进）

| 周期 | 任务 |
|---|---|
| Week 1 | 初始化项目，接入 Supabase，设计数据模型 |
| Week 2 | 接入 TwitterAPI.io + 官方博客 RSS，数据采集入库 |
| Week 3 | DeepSeek 处理管道（摘要/评分/标签/信号提示/观点过滤/领域判断） |
| Week 4-5 | 信息流页面（社交动态+官方新闻双tab）+ 搜索 |
| Week 6 | 用户登录 + Web Push + 每日简报 |
| Week 7 | 分享卡片（动态文案+二维码引流）+ H5 落地页 + 微信配置 |
| Week 8 | 发现页（单搜索框+双tab推荐）+ 整体打磨 |

两个 go/no-go 检查点：
- Week 3：数据管道和 AI 摘要质量是否过关
- Week 5：自己每天是否真的会打开信息流

---

*本文档为最终确认版，涵盖所有讨论迭代结果，可直接作为开发起点。*
