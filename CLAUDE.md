# FameScope — 项目说明（给 Claude Code 的上下文）

> 这是项目的总纲。Claude Code 每次启动会自动读取本文件。
> 详细的产品功能清单见同目录的 `FameScope_产品功能清单.md`，开发前请先通读。

## 这是什么

FameScope 是一个**手机端 PWA 网站**，帮用户每天 5 分钟掌握自己关注领域里最有影响力的人都说了什么、透露了什么信号。不只是信息聚合，而是用 AI 提炼"信号"。支持任意领域（AI、加密、太空科技等），不只限于 AI。

## 当前阶段

从零搭建 MVP。目标先跑通核心主链路：**数据采集 → 存入数据库 → AI 处理 → 前台信息流展示**。

已具备的资源：
- DeepSeek API key ✓
- TwitterAPI.io API key ✓（第三方 X 数据服务，直接接入真实数据，不用 mock）

## 技术栈（已确定）

- 前端/后端：Next.js 14（App Router）+ TypeScript
- 样式：Tailwind CSS（移动优先）
- 数据库：Supabase (Postgres)，含内置 Auth
- AI：DeepSeek API，模型 `deepseek-v4-flash`，使用 OpenAI 兼容端点
- X 数据：TwitterAPI.io（真实接入），但**封装成可替换的 `DataSource` 接口**，将来想换数据源时不影响其他代码
- PWA：next-pwa 或 Serwist
- 部署目标：Vercel + 自有域名

## AI 处理逻辑（三种内容，三种不同处理方式）

**核心原则：推文保留原味，新闻压缩摘要，简报按人提炼。**

### 1. 推文（tweet）— 直译，不提炼

- **只做中文直译**，不总结、不提炼、不加"为什么值得看"
- 目标是让用户看到原文的中文版，原汁原味，不失真
- 翻译要自然流畅，口语化内容保持口语感，不要书面化处理
- 保留原文入口供用户核对
- AI 仍需打：重要性评分（1-10）、话题标签、领域归属——但这些是系统内部用的，**不在卡片上展示"为什么值得看"信号提示**
- 观点过滤（判断回复/引用是否含实质观点）照常执行

### 2. 官方新闻（article）— AI 提炼摘要

- 原文篇幅长（几千字），需要 AI 压缩
- 生成中文摘要：**目标 150-200 字，硬上限 200 字**
- 摘要要交代清楚"这篇文章说了什么"，按内容繁简弹性调整，不注水
- 保留原文跳转链接
- 新闻卡片**可以加信号提示**（因为是正式公告，信号价值更明确）

### 3. 每日简报 — 按人物维度提炼

- 简报是 AI 发挥的主战场，但提炼方式是**按人物分节**，不是混合总结
- 结构：每个用户关注的人物单独一个小节，梳理其过去24小时内说了什么有价值的事情
- 示例结构：
  ```
  今日核心（一句话，整体最重要的信号）
  
  马斯克·Elon Musk
  → 发布 Grok 新版本演示，强调推理速度提升10倍
  → 回应竞争对手，暗示 xAI 下一步计划
  
  奥尔特曼·Sam Altman  
  → 透露下一代模型将强化长期记忆能力
  
  官方新闻摘要
  → Anthropic 发布新版本…（50字以内）
  ```
- 每个人物节点只列有实质价值的内容，过滤掉水推和无观点互动
- 简报里的内容是 AI 的判断和提炼，有观点，不是流水账

### 提示词管理
所有 DeepSeek 提示词集中放在 `lib/prompts.ts`，三种处理方式对应三套提示词，方便独立调整。
## 命名规范（重要）

- 公司名一律保留英文：OpenAI、Anthropic、Meta、xAI、Google DeepMind，**不翻译**
- 人物用通用中文译名 + 英文名：奥尔特曼 · Sam Altman、哈萨比斯 · Demis Hassabis、马斯克 · Elon Musk、杨立昆 · Yann LeCun、阿莫迪 · Dario Amodei
- 显示格式：中文译名在前，英文名在后

## 信源（source）—— 核心概念

追踪对象统称"信源"，分两类，数据模型用一个字段区分：
- **个人（person）**：创始人、科学家等个人账号。其即兴的回复/引用最有观点价值，需走观点过滤。
- **官方（org）**：公司/机构官方账号（如 @OpenAI、@AnthropicAI、@GoogleDeepMind）+ 公司官方博客 RSS。基本是正式公告，每条都重要，不需要"有没有观点"的过滤。

注意：之前若把表命名为 `person`，应泛化为 `source`，加 `type` 字段（person / org）。两类用同一套采集逻辑，多接几个账号成本很低。

**不预置任何信源**——产品支持任意领域，不默认塞 AI 公司。一切由用户自己添加。

## 内容类型与信息流卡片（两种样式）

信息流分两个 tab：**社交动态**（X 推文）和**官方新闻**（官博文章）。两种阅读心态分开，长新闻卡片不打断刷推文的节奏。每个 tab 标题带未读数气泡，各自独立的过滤状态。社交动态 tab 的过滤含"含观点"，官方新闻 tab 不含（新闻都是正式内容，无需此过滤）。

采集来的内容有两种形态，由 `content_type` 字段决定进哪个 tab、渲染哪种卡片：

1. **X 推文（tweet）**：短、口语化。卡片含头像、人名/职位、关系标注（原创/引用/回复）、推文正文、操作栏。
2. **官网新闻（article）**：来自官方博客 RSS，长、正式、有标题正文结构。卡片形态不同：
   - 右上角用"新闻"标记替代关系标注（博客没有原创/回复概念）
   - **有标题**（大字号突出，这是与推文最大的区别）
   - 标题下是 AI 生成的中文摘要——**绝不在信息流展示全文**（全文太长会撑爆卡片，也违反"只存摘要+链接"原则）
   - 可选一张配图（引用原图 URL）
   - 明确的"阅读全文"链接跳转官网

两种卡片共享：顶部"为什么值得看"信号提示、底部操作栏（原文/收藏/分享）。机构用方角头像、个人用圆头像区分。

对 AI 处理的影响：新闻类内容要额外提取"标题"，并生成一段中文摘要交代清楚文章讲了什么——**摘要控制在 200 字以内（目标 150-200 字，硬上限 200 字）**，根据内容繁简弹性调整（简单公告短一点，复杂发布用满，不要硬凑字数注水）。目标是让用户扫一眼就能判断要不要点进去看全文。推文摘要保持简短（一两句话即可，不受 200 字目标约束）。

两者都另有一句话的"为什么值得看"信号提示：一句话点出价值，摘要交代内容，二者配合。

## 发现页（用户如何添加信源）

**单一搜索框**，系统自动判断用户输入的是"领域"还是"具体名字"，给出不同形态的结果。不要做成两个搜索框（体验差，用户会纠结该用哪个）。

- 输入被识别为**领域**（如 AI、加密货币、太空）→ 走推荐流程，结果用**双 tab 区分**：
  - "名人" tab：个人信源
  - "官方账号" tab：公司/机构信源
  - 两个 tab 都按 影响力 / 相关性 / 活跃度 三维度排序
- 输入被识别为**具体名字**（如"马斯克""OpenAI"）→ 直接匹配对应账号，个人和官方混合显示

判断逻辑：命中已知领域词或宽泛类别词 → 领域；具体人名/公司名 → 名字。搜索框上方显示一行提示告诉用户当前识别为哪种（透明、可纠错）。边界情况（既像领域又像名字）：结果区给一个切换出口，如"想看整个 X 领域？按领域推荐"。

另有"我的追踪"列表用于管理已添加的信源。

## 领域推荐机制（必须按两步法实现）

无论名人还是官方账号，推荐都**绝不能只让 AI 凭空生成名单**（会编造账号、瞎报粉丝数、给出不存在的 handle）。必须两步：

1. **AI 召回候选名单**：让 DeepSeek 列出该领域有影响力的个人/公司，只取"名称 + 大致身份 + 通用中文译名"，**不信任它给出的任何数字或账号**。
2. **真实数据核实 + 打分**：拿这些名称去 TwitterAPI.io 查真实账号——
   - 确认账号真实存在，查不到的候选直接丢弃（过滤 AI 幻觉）；官方账号通常有认证，更易确认
   - 抓取真实粉丝数、认证状态、近期发帖
   - 用真实数据计算三维度分值并排序：影响力（粉丝/认证/媒体提及）、相关性（简介与发帖跟领域的匹配度）、活跃度（近30天发帖频率）

常见领域（AI、加密、太空等）的推荐结果可缓存，避免每次搜索都重跑整套流程。

## 建议的开发顺序（请严格按此推进）

**两个阶段不能混，UI 全部用户确认后才能进入数据对接。**

### 第一阶段：UI 开发（全部用静态假数据，不调任何接口）

1. 用 `create-next-app` 初始化 TypeScript + Tailwind 项目，配置好设计 token（参考 UI 设计规范章节），做任何 UI 前先调用 `frontend-design` 技能
2. 按以下顺序逐页完成 UI，每页做完等用户确认再继续：
   - 信息流主页（社交动态 + 官方新闻双 tab，假数据填充）
   - 简报页（当日简报 + 历史列表）
   - 发现页（单搜索框 + 双 tab 推荐）
   - 个人设置页（追踪管理 + 实时提醒开关 + 深浅色切换）
   - 分享卡片弹层（中英对照 + 品牌顶部 + 二维码）
   - 登录/注册页（手机号验证码）
   - 新用户引导页（空状态）
3. 所有页面用户确认无误，进入第二阶段

### 第二阶段：数据对接（UI 全部确认后才开始）

1. **接入 Supabase**：建好数据表，作为数据落地的地基——必须在采集之前就位
2. 设计数据模型——必须拆成两层：
   - **内容层（所有用户共享）**：`source` 表（type/英文名/中文译名/公司/X账号/官博RSS/三维分值）；`post` 表（source_id/content_type/标题/原文/中文摘要/评分/标签/领域/关系类型/媒体URL）— **post 表不含收藏**
   - **用户行为层（每人独立）**：`user_source` 表（user_id + source_id + notify + followed_at）；`user_post_state` 表（user_id + post_id + is_starred）；`digest` 表（user_id + generated_at + content）
   - 原因：多人共订同一信源时，post 只存一份、AI 只处理一次；收藏是私有状态，A 的操作不能影响 B
3. 实现 `DataSource` 接口 + `TwitterApiIoDataSource`（接入真实 TwitterAPI.io），采集个人+官方账号的原创/回复/引用推；官方博客 RSS 也作为数据源接入，存入 Supabase
4. 实现 DeepSeek 处理管道（摘要+评分+标签+领域判断+信号提示+观点过滤）
5. 把第一阶段 UI 里的假数据全部替换为真实接口调用
6. 跑起来，验证真实采集 + AI 处理 + 前台展示全链路通畅

完成后再依次接入：用户登录、每日简报生成、推送、分享卡片生成、发现页推荐两步法。
## 媒体处理原则（控制服务器成本）

只存文本和链接，**绝不下载/转存图片和视频**。图片直接引用原 URL + 懒加载；视频只存封面图 URL，点击才跳转或嵌入原平台播放器。

## UI 设计要求

设计分两个阶段，不要在 MVP 主链路还没跑通时就死磕视觉：

**开发期间（搭功能时）**：遵循基本规范，避免欠债——
- 移动优先（这是手机端 PWA，所有交互按拇指操作设计）
- 统一的设计 token：一套配色、间距、圆角、字体，集中管理，不要每个页面各搞一套
- 做任何前端 UI 时，**先调用并遵循内置的 `frontend-design` 技能**（它讲如何做出有设计感、不像默认模板的界面），不要直接套 Bootstrap/默认样式糊过去

**主链路验证通过后**：专门做一轮完整的视觉设计提升——
- 这是要分享传播、给真实用户用的公开网站，界面粗糙会劝退人
- 定义完整设计语言：配色系统、字体搭配、间距规范、图标风格、必要的动效
- 重点打磨：信息流卡片、分享卡片（朋友圈传播的门面）、发现页

参考方向（非最终）：紫色为品牌主色（信号/雷达意象），分享卡片按领域切换配色。最终设计在这一轮专门确定。

**MVP 阶段不做独立后台。** 自用初期内容和用户都少，不值得花时间建后台：
- DeepSeek 提示词：放配置文件，改完重新部署即可
- 偶尔要改的数据：直接用 Supabase 自带的可视化表格界面查看/编辑
- 采集是否正常、内容质量：自己刷网站就能发现

未来扩展：等内容量和用户多到手动管不过来时，再针对性地做后台（采集监控、内容审核等），现阶段不列入开发范围。

## 不要做的事

- 不要把 X 数据采集写死成 TwitterAPI.io，必须经过 DataSource 接口
- 不要让 AI 直接生成人物池名单，必须用真实数据核实（见上）
- 不要下载或转存任何媒体文件
- 暂不开发付费/会员系统、独立后台
- API key 放 `.env.local`，不要提交到 git

## 推送策略（PWA Web Push）

不做邮件推送。推送分两种，都通过 PWA Web Push 发出：

**1. 实时提醒（per 信源，用户自主开关）**
- 用户可以对每个订阅的信源单独开启"实时提醒"
- 逻辑：采集到新内容 → AI 评分 → 检查订阅了该信源且开启 notify 的用户 → 给这些用户发 Web Push
- 不依赖 Vercel Cron 频率，内容采集完立即触发检查
- 数据模型：`user_source` 表加 `notify boolean` 字段

**2. 每日简报（每天早8点，Vercel Cron）**
- 每天定时给所有用户各生成一份个性化简报，Web Push 通知
- 简报生成步骤（**必须设计成 per-user 函数**，不能一次性生成所有人的）：
  1. 根据该用户的 `user_source` 拉取过去24小时内的相关 post
  2. 按评分筛选重点（≥6分），控制在10-15条以内
  3. 调用 DeepSeek 生成结构化简报：
     - 今日核心（一句话结论）
     - 最值得关注（2-3条高分动态）
     - 信号共振（多人同时提到同一话题，自动检测）
     - 官方新闻（article 类型的重点）
     - 你关注的人（按信源逐个列出今日动态数）
  4. 简报存入数据库（用户可回看历史）
  5. Web Push 发出通知，用户点击进入简报页

- 为什么必须 per-user：每个用户订阅不同 → 第1步拉出的数据不同 → 简报内容天然个性化
- 性能注意：用户多时 Cron 里要做任务队列，不能同步跑完所有用户。现在用户少可以先简单实现，但函数粒度从一开始就是 per-user

## 历史数据与简报归档逻辑

### 订阅后的历史内容展示

用户订阅一个信源后，**回溯展示过去7天的历史内容**（不是只从订阅时刻开始，也不是展示全部历史）：
- 原因：只从订阅时刻起 → 信息流空白，第一印象差；全量历史 → 被旧内容淹没
- 实现：`user_source` 表记录 `followed_at`，查询时条件为 `post.created_at >= followed_at - 7天`
- 7天之前的内容不展示在信息流，但全文搜索可以检索到（搜索不受7天限制）

### 简报归档

简报永久保留，不删除，用户可随时回看历史。

新增 `digest` 表：
```
digest
- id
- user_id
- generated_at   （生成时间）
- content        （简报正文，JSON 结构化存储）
```

简报页面展示为列表：最新一期默认展开在顶部，往下滑是历史简报（按日期倒序）。每次 Cron 生成新简报后旧的自动归入历史，不覆盖不删除。


## UI 设计规范（Claude Code 执行用）

**做任何前端 UI 之前，必须先读取并遵循内置的 `frontend-design` 技能。** 这是强制要求，不是建议。

### 设计优先原则
先把所有页面 UI 做完并确认，再对接数据接口。不要边做 UI 边接数据，避免来回返工。

### 双色模式
浅色为默认，用户可切换深色。两套必须同时支持，所有颜色必须用 CSS 变量，**严禁硬编码 hex 颜色值**（否则深色模式下会不可见）。正确做法：`color: var(--color-text-primary)`，错误做法：`color: #333`。

### 品牌色系（从 frontend-design 色板取值）
- 品牌主色浅色：Purple 600 → `#534AB7`
- 品牌主色深色：Purple 400 → `#7F77DD`
- 信号提示：Amber 系，用 `var(--color-background-warning)` + `var(--color-text-warning)`
- 收藏激活：Amber 系
- 分享按钮：用 `var(--color-background-info)` + `var(--color-text-info)`

### 卡片操作栏（三个图标按钮，无文字）
三个按钮：原文（`ti-language`）、收藏（`ti-star`）、分享（`ti-share-3`）。
- 纯图标，无文字标签
- 右对齐排列，每个按钮 38×38px，`border-radius: var(--border-radius-md)`
- 默认态：`background: transparent; color: var(--color-text-secondary)`
- 收藏激活：`background: var(--color-background-warning); color: var(--color-text-warning)`
- 分享：`background: var(--color-background-info); color: var(--color-text-info)`

### 信息流卡片结构（从上到下）
1. 头部：未读圆点（6px，品牌紫）+ 头像（36px 圆形/方角）+ 姓名·英文名 + 职位·时间 + 关系标签（右对齐）
2. 信号提示：左侧 2px 竖线 + 琥珀背景，灯泡图标 + 一句话（**仅新闻卡片显示，推文卡片不显示**）
3. 正文摘要（13px，line-height 1.65）
4. 引用块（可选，内嵌卡片）
5. 媒体（可选，封面图+播放按钮）
6. 标签行
7. 操作栏（分隔线 + 三图标右对齐）

### 头像规范
- 个人信源：圆形头像，`background: var(--color-background-info); color: var(--color-text-info)`
- 官方信源：9px 圆角方形头像，与圆形区分


### 信息流页面结构
- 顶部栏：品牌 Logo（26px 方形徽章 + FameScope 字样）+ 右侧搜索图标
- 主 tab：社交动态 / 官方新闻（下划线式，激活用品牌紫）
- 过滤栏：全部 / 收藏 / 含观点（胶囊式，激活用品牌紫实底白字）
- 底部导航栏：信号流 / 简报 / 发现 / 我的

### 页面清单（按此顺序完成 UI）
1. 信息流主页（社交动态 tab + 官方新闻 tab）
2. 简报页（当日简报 + 历史列表）
3. 发现页（单搜索框 + 名人/官方双 tab 推荐 + 我的追踪）
4. 个人设置页（追踪列表管理 + 实时提醒开关 + 深浅色切换）
5. 分享卡片弹层（中英对照 + 品牌顶部 + 二维码底部）
6. 登录/注册页（手机号验证码）
7. 新用户引导页（空状态 + 引导添加第一个信源）

### 字体层级
- 品牌名：17px / 500 / letter-spacing -0.02em
- 人名：13px / 500
- 正文：13px / 400 / line-height 1.65
- 新闻标题：15px / 500 / line-height 1.4
- 分享金句：serif 字体 / 15px / line-height 1.55
- 辅助/标签：11px / 400
- 极小标注：10px / 400（最小不得低于此）

### 间距规范
- 页面水平边距：16px
- 卡片内边距：14px
- 卡片间距：10px
- 组件内部间距：8px / 12px
- 底部导航高度：留足安全区

### 圆角规范
- 卡片：`var(--border-radius-lg)`（12px）
- 按钮/操作项：`var(--border-radius-md)`（8px）
- 胶囊标签：20px
- 关系标签：6px
- 个人头像：50%（圆形）
- 官方头像：9px
- 品牌徽章：8px
